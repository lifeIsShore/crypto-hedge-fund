# Research layer dependencies
# Install with: pip install -r requirements.txt

# Core (likely already installed from portfolio layer)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data
yfinance>=0.2.0
pandas-datareader>=0.10.0   # Fama-French factor data (Kenneth French's website)

# Modelling
statsmodels>=0.14.0          # OLS regressions, Granger causality, cointegration
scikit-learn>=1.3.0          # Clustering, regime classification
PyPortfolioOpt>=1.5.5        # Black-Litterman implementation

# Visualisation (notebooks)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0               # Interactive heatmaps in notebooks

# Notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
